version: 2

project_name: petri-operator

before:
  hooks:
    - go mod tidy
    - make manifests generate
    - bash -c 'kustomize build config/crd > /tmp/crds.yaml'

builds:
  - id: operator
    main: ./cmd/operator
    binary: operator
    env: [CGO_ENABLED=0]
    goos: [linux, darwin]
    goarch: [amd64, arm64]
    ldflags:
      - -s -w
      - -X github.com/petri-dev/petri-operator/internal/version.Version={{.Version}}
      - -X github.com/petri-dev/petri-operator/internal/version.Commit={{.Commit}}

  - id: deployer
    main: ./cmd/deployer
    binary: deployer
    env: [CGO_ENABLED=0]
    goos: [linux]
    goarch: [amd64, arm64]
    ldflags:
      - -s -w
      - -X github.com/petri-dev/petri-operator/internal/version.Version={{.Version}}
      - -X github.com/petri-dev/petri-operator/internal/version.Commit={{.Commit}}

dockers_v2:
  - id: operator
    ids: [operator]
    image_templates:
      - ghcr.io/petri-dev/petri-operator:{{.Tag}}
      - ghcr.io/petri-dev/petri-operator:latest
    build_flag_templates:
      - --build-arg=BIN=operator
      - --label=org.opencontainers.image.version={{.Version}}
      - --label=org.opencontainers.image.revision={{.Commit}}
      - --label=org.opencontainers.image.source=https://github.com/petri-dev/petri-operator
    platforms:
      - linux/amd64
      - linux/arm64

  - id: deployer
    ids: [deployer]
    image_templates:
      - ghcr.io/petri-dev/petri-deployer:{{.Tag}}
      - ghcr.io/petri-dev/petri-deployer:latest
    build_flag_templates:
      - --build-arg=BIN=deployer
      - --label=org.opencontainers.image.version={{.Version}}
      - --label=org.opencontainers.image.revision={{.Commit}}
      - --label=org.opencontainers.image.source=https://github.com/petri-dev/petri-operator
    platforms:
      - linux/amd64
      - linux/arm64

archives:
  - id: operator
    ids: [operator]
    name_template: petri-operator_{{.Version}}_{{.Os}}_{{.Arch}}

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"
      - Merge pull request
      - Merge branch

release:
  draft: true
  name_template: "v{{.Version}}"
  extra_files:
    - glob: /tmp/crds.yaml
